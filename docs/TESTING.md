# 测试与验收

## 最新自动检查

2026-09-25：

- TypeScript 严格类型检查通过，0 错误。
- Cocos Creator 3.8.8 Web Mobile 构建通过，返回码 36。
- Cocos Creator 3.8.8 抖音小游戏构建通过，返回码 36。
- 抖音构建产物包含 `game.js`、`game.json`、`project.config.json`，方向为 `portrait`，验证包约 3.19 MiB。
- 构建产物使用 750 × 1334 设计分辨率，启动场景为 `assets/scenes/battle/scn_battle.scene`。
- 构建场景正确包含 `GameRoot` 组件，不存在缺失脚本。
- 《叮咚！夜班开始》名称更新后重新通过 TypeScript、Web Mobile 与抖音小游戏构建；产物包含新首屏标题，抖音工程名为 `ding-dong-night-shift`。
- 轻量玩法基础闭环更新后再次通过 TypeScript 严格检查、Web Mobile 和抖音小游戏构建；抖音验证包约 3.20 MiB。
- 抖音产物包含 `game.js`、`game.json`、`project.config.json`，`deviceOrientation` 为 `portrait`。
- 使用等比例竖屏视口完成可视检查：顶部状态、6 个塔位、店员/升级行和三个道具按钮均在首屏内，无文字裁切。
- 实际点按验证通过：建造店员、选择场上店员、升级、一键冷气、暂停和继续营业。
- 10 关地图配置审计通过：路线互不重复，每关 4～6 个塔位，塔位与道路中心线间距不少于约 50，塔位互距不少于约 78，所有塔位均达到最低有效攻击覆盖。
- 关卡压力审计通过：综合敌人总生命、速度与路线长度后，前 10 关整体递增且没有明显难度倒挂；第 5、10 关作为抢购潮节点保留更明显提升。
- 新建造交互点按通过：点击空塔位、在塔位旁选择店员、建造后显示范围、点击上下文按钮升级、切换另一塔位。
- 首关经济节点实测：170 初始零钱可建两座豆包，第一波无道具收入为 64，恰好覆盖豆包二级升级费 64。
- 正式战斗倍速通过等比例竖屏点按验证：顶部按钮可在 1、2、3 倍之间循环，暂停时可切换且战斗不推进，刷新后保留选择。

2026-09-26：

- 750 × 1334 基准浏览器视口中，浏览器内容区、Canvas 像素尺寸和 CSS 尺寸均为 750 × 1334，画面横纵使用相同缩放比例。
- 375 × 667 手机等比视口中，浏览器内容区与 Canvas CSS 尺寸均为 375 × 667，顶部状态、地图、塔位和底部道具完整显示。
- 手机视口点击左侧塔位后正确出现跟随塔位的豆包建造按钮，等比缩放后的触控命中通过。
- 首波教学提示结束后在怪物入口显示“下一波 1/3”和数字倒计时；数字归零后提示消失、波次切换为 1/3 并开始出怪。
- 前 3 关配置审计通过：综合压力依次约为 0.9、1.4、1.7，第 4 关约为 2.2；不存在前三关难度倒挂。
- 前 3 关经济节点通过静态核对：第 1 关初始 170 可建两名豆包，第 2 关初始 195 可建豆包与棉棉，第 3 关初始 235 可建布丁与豆包。
- 375 × 667 等比视口中完成豆包建造与升级点按，建造/升级浮动文字、攻击朝向、炮口火光和投射物显示正确，浏览器控制台无错误或警告。
- Web Mobile 构建成功导入 11 个原创 WAV 音效，`resources` 配置包含全部音频路径；运行时资源加载未产生控制台错误。
- Cocos Creator 3.8.8 抖音小游戏构建完成，任务输出 `Finished`；产物约 3.37 MiB，方向为 `portrait`，并包含全部 11 个音效资源路径。
- 前 3 关高密度配置审计通过：每关 16 个潜在建造单元、8 个开局空位、8 个有效障碍物；道路间距不少于 50，建造单元最小互距不少于 60，均达到最低攻击覆盖要求。
- 新版基础压力依次约为 1.5、2.4、2.8；前 3 关均为 4 波，敌人总数分别为 34、51、64，未出现前三关难度倒挂。
- 750 × 1334 竖屏点按验证通过：空地建造、塔旁升级与出售、65% 返还、点选障碍物、障碍物受击与清除、获得零钱、原位释放建造单元和自动打开建造菜单均正常。
- 教学横幅结束后完整显示入口位置的 4、3、2、1 倒计时，计时期间仍可建造和指定清障；横幅显示期间倒计时不会后台减少。
- 新版紧凑 UI 在 750 × 1334 视口内完整显示；最左侧塔位的上下文按钮仍可点击，建造/出售浮动文字已夹紧到安全区内。
- 新版 Web Mobile 运行期间浏览器控制台无错误或警告。
- 新版抖音小游戏构建任务输出 `Finished`；验证包共 53 个文件、约 3.37 MiB，`game.json` 保持 `deviceOrientation: portrait`。
- GM 调试版在 750 × 1334 视口完成点按验证：右上角入口、面板打开/关闭、1～10 关按钮、当前关高亮和“返回正式进度”均正常。
- 实际从正式第 2 关跳转到 GM 第 10 关，顶部正确显示“第 10 关·GM”；返回正式进度后恢复第 2 关，浏览器控制台无错误或警告。
- GM 面板开启期间游戏逻辑停止推进，面板外点击不会穿透到建造、道具或战场操作。
- GM 版本通过 Cocos TypeScript 项目检查（引擎声明使用 `skipLibCheck`）和 Web Mobile 调试构建。
- 抖音小游戏发布构建输出 `Finished`，共 53 个文件、约 3.37 MiB、保持 `portrait`；构建产物检索不到 GM 面板文案和入口标识。

自动检查使用的临时配置、日志和 Web 构建产物位于 `G:\gptwork`，不属于项目仓库。

Creator 命令行日志存在 Electron 缓存写入和默认 `merge_dep` 参数提示，但构建任务均输出 `Finished`，所需产物完整生成。这些提示不作为发布验收结论，后续在 Creator 构建面板配置正式参数后复核。

当前抖音验证包使用 `testappId`。取得正式 App ID 和激励视频广告位 ID 后，必须重新构建并在抖音开发者工具与真机上验收。

## 人工试玩清单

- [x] 首屏竖屏布局完整，无文本裁切。
- [x] 750 × 1334 基准画布完整，375 × 667 等比手机视口关键操作不被裁切；16:9 至 20:9 真机仍待验证。
- [x] 第 1 关豆包可建造；前 3 关逐步解锁另外两名店员的配置已验证。
- [x] 点击空塔位后显示可建店员；点击已建造店员可显示攻击范围和上下文升级按钮。
- [x] 前 3 关每关 16 个潜在建造单元、8 个障碍物和 8 个开局空位通过配置审计与竖屏可视检查。
- [x] 点击障碍物可指定清理；无紧急敌人时范围内店员攻击障碍物，清除后获得零钱并在原位开放建造。
- [x] 点击已建店员可在上下文菜单中出售，并按累计投入的 65% 返还零钱。
- [x] 调试构建可通过 GM 面板直接进入第 1～10 关，并可返回正式存档关卡。
- [x] GM 切关、GM 通关不修改正式解锁进度；抖音发布构建不包含 GM 入口。
- [x] 10 关路线、塔位道路间距、塔位互距、攻击覆盖和基础压力曲线通过自动审计。
- [ ] 普通、疾行、重甲敌人可沿路径到达终点。
- [ ] 豆包单体、棉棉减速、布丁范围伤害的最终数值符合预期。
- [x] 暂停与继续按钮可用；暂停遮罩和按钮文字显示正确。
- [x] 1、2、3 倍速可循环切换，低帧率下使用稳定子步推进；切关、重开、复活和再次启动保留选择。
- [x] 首波和波间出怪前显示入口位置、下一波编号和数字倒计时；倍速与暂停规则正确。
- [x] 建造、升级、攻击、命中、击杀和漏怪具备相互区分的视觉反馈。
- [x] 11 个短音效已被 Cocos 导入，运行时路径与加载失败降级逻辑通过构建和浏览器无报错验证。
- [ ] 在 Creator 预览和抖音真机中人工确认各音效音量、频率与手机外放听感。
- [ ] 第 2、3 关完成整局人工通关，确认棉棉与布丁定位及目标时长。
- [ ] 失败后复活每局最多一次，重开可正常清空状态。
- [ ] 第 10 关最后一波完成后出现胜利结算，并可再次挑战。
- [ ] 宽屏、长屏和异形屏没有关键按钮落到安全区之外。

## 抖音真机清单

参见 [DOUYIN_RELEASE.md](./DOUYIN_RELEASE.md)。广告 API 在普通浏览器和 Creator 模拟器中不能替代真机验收。
