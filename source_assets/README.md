# 生产源文件

这里存放不会直接进入 Cocos 运行包的可编辑源文件：

- `art/`：PSD、AI、原始插画和导出说明。
- `animation/`：AEP、Spine 工程及动画源文件。
- `audio/`：WAV/FLAC 母带、工程文件和授权记录。

进入游戏的导出资源放入 `assets/art/` 或 `assets/audio/`。临时转换文件、批处理脚本、验证包和日志统一放在非系统盘的 `gptwork` 工作区，不得提交到本目录。
